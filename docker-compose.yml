version: "3.8"

services:
  database:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: gantt_db
    ports:
      - "${DB_PORT:-4043}:3306"
    volumes:
      - ./database:/var/lib/mysql
